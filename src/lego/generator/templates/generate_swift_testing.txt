You are an expert iOS test engineer writing Swift Testing unit tests
(the framework introduced in Xcode 16, not XCTest).

TARGET FILE:
{target_content}

RELATED FILES (protocols, models, dependencies):
{related_contents}

TESTABILITY ANALYSIS:
{analysis_summary}

Generate a COMPLETE, COMPILABLE Swift Testing test file for {class_name},
specifically testing these methods: {method_list}

REQUIREMENTS:
- Start with:
    import Testing
    import Foundation
    @testable import {module_name}
- Use @Suite("{class_name}") on a struct named {class_name}Tests.
- Each test is an instance method annotated with @Test("description").
- Method naming: methodName_scenario_expectedBehavior() — NO "test" prefix.
- Use Swift Testing assertions (NOT XCTest):
  - #expect(value == expected)
  - try #require(optional) for unwrapping (replaces XCTUnwrap / force-unwrap)
  - #expect(throws: SomeError.self) { try sut.something() }
  - For async work: use async test methods and await directly.
- Create mock classes/structs for ALL dependencies using protocols.
  - Each mock should track method calls (e.g., `var fetchUserCalled = false`).
  - Each mock should allow configuring return values.
- Use `init()` on the suite struct to create the SUT + mocks. Each test gets
  a fresh suite instance, so there's no shared state to tear down.
- For each method, include tests for:
  - Happy path with valid inputs
  - Nil or empty inputs where applicable
  - Error conditions and failure paths
  - Boundary values (empty strings, zero, negative numbers, max values)
- NEVER force unwrap (!) — use `try #require(...)` or `guard let`.
- NEVER use real network, file system, or UserDefaults — everything mocked.
- NEVER test private methods directly.

Output ONLY the complete .swift test file. No markdown fences.
No explanation. No preamble. Just the Swift code.
